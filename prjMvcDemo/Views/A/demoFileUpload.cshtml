
@{
    ViewBag.Title = "demoFileUpload";
}

<h2>demoFileUpload</h2>

@using (Html.BeginForm("demoFileUpload", "A", FormMethod.Post,
    new { enctype = "multipart/form-data" }))
{
    <input type="file" name="photo"/>
    <input type="submit" value="OK" />
}